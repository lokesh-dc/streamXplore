import React, { ReactElement } from "react";

interface props {}

const VideosComponent: React.FC<props> = ({}): ReactElement => {
	return <div>Video section</div>;
};

export default VideosComponent;
